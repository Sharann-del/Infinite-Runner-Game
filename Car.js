class Car{
     constructor(x,y,width,height){
             x = 400;
             y = 200;
             this.width = width;
             this.height = height;
             if(keyCode === RIGHT_ARROW){
             this.x = this.x+5;
             camera.position.x = x;
             camera.position.y = y;
        }    
    }
}